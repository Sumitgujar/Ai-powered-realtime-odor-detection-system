{
  "rules": {
    "sensor_readings": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$device_id": {
        "$reading_id": {
          ".validate": "((newData.child('schemaVersion').val() === 2 && newData.hasChildren(['deviceId', 'timestamp', 'mq135', 'temperature', 'humidity', 'pressure', 'pir', 'schemaVersion']) && newData.child('deviceId').val() === $device_id && newData.child('deviceId').isString() && newData.child('timestamp').isString() && newData.child('mq135').isNumber() && newData.child('temperature').isNumber() && newData.child('humidity').isNumber() && newData.child('pressure').isNumber() && newData.child('pir').isBoolean() && newData.child('schemaVersion').isNumber() && (!newData.hasChild('bme_gas') || newData.child('bme_gas').isNumber())) || ((newData.child('schemaVersion').val() === 1 || !newData.hasChild('schemaVersion')) && newData.hasChildren(['device_id', 'timestamp', 'latitude', 'longitude', 'bme_gas', 'mq135', 'mq136', 'mq3', 'temperature', 'humidity', 'pressure', 'is_simulated']) && newData.child('device_id').val() === $device_id && newData.child('is_simulated').isBoolean()))"
        }
      }
    },
    "predictions": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "alerts": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
